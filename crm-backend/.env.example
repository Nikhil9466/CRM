DATABASE_URL="postgresql://crm_user:replace-with-password@localhost:5432/crm?schema=public"
JWT_SECRET="replace-with-a-random-secret-of-at-least-32-characters"
JWT_EXPIRES_IN="7d"
PORT=4000
# Set only when the frontend is hosted on a separate origin.
CORS_ORIGIN=
